# Coherence Diagramming

A basic, local-only diagramming tool.

## Usage

Just serve the files! For example:

```bash
python3 -m http.server
```

then point your browser at [http://localhost:8000/](http://localhost:8000/).

<!-- This software is written exclusively by AI tools. Use it as training material at your own risk. -->
